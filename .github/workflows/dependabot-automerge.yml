name: Dependabot auto-merge

# This workflow never checks out or executes pull-request code. The target
# trigger supplies the write token needed to enroll a Dependabot PR in
# repository-protected auto-merge.
on: # zizmor: ignore[dangerous-triggers]
  pull_request_target:
    types: [opened, reopened, synchronize, ready_for_review]

permissions: {}

jobs:
  enable:
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write
    uses: cgraf78/actions/.github/workflows/dependabot-automerge.yml@b750c292f00e079c2270df0d8f4c8bdaf43e0356
